spring:
  data:
    redis:
      host: 192.168.2.15 # Redis服务器地址
      database: 0 # Redis数据库索引（默认为0）
      port: 31721 # Redis服务器连接端口
      password: redis123 # Redis服务器连接密码（默认为空）
      timeout: 3000ms # 连接超时时间（毫秒）
logging:
  level:
    root: info
    com.macro.mall: debug
# logstash:
#   host: localhost