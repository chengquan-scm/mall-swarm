## 🔌 Auth Service 配置
apiVersion: v1
kind: Service
metadata:
  name: mall-auth
  namespace: master
  labels:
    app: mall-auth
    version: "master-7e03adf-97"
spec:
  type: ClusterIP
  selector:
    app: mall-auth
  ports:
    - name: http
      port: 8401
      targetPort: 8401
      protocol: TCP
