# 全局配置
global:
  namespace: master
  imagePullSecrets: harbor-secret
  timezone: Asia/Ho_Chi_Minh

# 镜像配置
image:
  repository: core.harbor.domain/mall-swarm/master
  tag: master-4071364-104
  pullPolicy: IfNotPresent

# 各服务副本数配置
replicaCount:
  admin: 2
  auth: 2
  gateway: 2
  portal: 2
  search: 1