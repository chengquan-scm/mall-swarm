apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mysql.fullname" . }}-initdb
  namespace: {{ .Release.Namespace }}
data:
  {{ .Values.initdb.sqlFile }}: |-
{{ .Files.Get "files/mall.sql" | indent 4 }}
  my.cnf: |-
{{ .Values.configuration | indent 4 }}
